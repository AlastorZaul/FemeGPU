apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  env.js: |
    (function (window) {
      window.__env = window.__env || {};
      // L'URL réelle de votre API en production
      window.__env.apiUrl = 'https://api.prod.votre-domaine.com';
    }(this));
